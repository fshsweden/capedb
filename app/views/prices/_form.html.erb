<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field readonly">
    <%= f.label :product %><br>
    <%= collection_select(:product, :product_id, Product.all, :id, :name, :prompt => 'Please select product') %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">

    <%= date_select("price", "pdatetime") %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
